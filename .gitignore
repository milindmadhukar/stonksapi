# ── Go / API ──────────────────────────────────────────────────────────────────
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories
# vendor/

# Cached Files
cached_files

# Go binary
/api/stonksapi

# ── Dashboard (Astro) ─────────────────────────────────────────────────────────
# build output
dashboard/dist/
# generated types
dashboard/.astro/

# dependencies
dashboard/node_modules/
# bun.lock is deliberately tracked: the Docker build runs
# `bun install --frozen-lockfile` from a clean checkout, so an ignored lockfile
# breaks CI and makes image builds non-reproducible.

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Vercel
dashboard/.vercel/

# ── Shared ────────────────────────────────────────────────────────────────────
.env
.DS_Store
.idea/
/tmp
/api/tmp
